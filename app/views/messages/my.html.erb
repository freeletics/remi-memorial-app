<p id="notice"><%= notice %></p>

<section class="messages-leave-btn">
  <%= link_to "Leave your message", new_message_path, class: "btn btn-default" %>
</section>

<ul class="messages-list">
  <%= render partial: "message", collection: @messages %>
</ul>
